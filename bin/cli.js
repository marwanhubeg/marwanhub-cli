#!/usr/bin/env node

const { program } = require('commander');
const { version } = require('../package.json');
const colors = require('../utils/colors');

// استيراد الأوامر الجديدة
const { 
  updateCommand, 
  toolsCommand, 
  contactCommand 
} = require('../src/commands/newCommands');

// رسالة الترحيب
console.log(colors.title(`
╔═══════════════════════════════════════════╗
║      🚀 Marwan Hub CLI - الإصدار ${version}     ║
║     أداة سطر أوامر رسمية لـ ماروان هوب     ║
╚═══════════════════════════════════════════╝
`));

// إعداد Commander الأساسي
program
  .name('mh')
  .description('أداة سطر أوامر لـ Marwan Hub - واجهة موحدة لجميع الخدمات')
  .version(version);

// إضافة الأوامر الجديدة
program.addCommand(updateCommand);
program.addCommand(toolsCommand);
program.addCommand(contactCommand);

// أمر المساعدة العربية
program
  .command('مساعدة')
  .alias('help')
  .description('عرض رسالة المساعدة بالعربية')
  .action(() => {
    console.log(colors.info(`
📖 مساعدة Marwan Hub CLI:

أوامر أساسية:
  mh مشاريع        - عرض مشاريع ماروان هوب
  mh خدمات         - عرض الخدمات المتاحة
  mh أدوات         - الأدوات والتقنيات
  mh تحديث         - تحديث الأداة لأحدث إصدار
  mh تواصل         - طرق التواصل مع الفريق

أوامر التطوير:
  mh جديد <اسم>    - إنشاء مشروع جديد
  mh تشغيل         - تشغيل بيئة التطوير
  mh اختبار        - تشغيل الاختبارات

أوامر المساعدة:
  mh مساعدة        - عرض هذه الرسالة (عربي)
  mh --help        - عرض المساعدة (إنجليزي)
  mh --version     - عرض رقم الإصدار

📞 للتواصل: support@marwanhub.tech
🌐 الموقع: https://marwanhub.tech
`));
  });

// أمر عرض المشاريع
program
  .command('مشاريع')
  .description('عرض مشاريع ماروان هوب على GitHub')
  .action(() => {
    console.log(colors.warning(`
📂 مشاريع ماروان هوب:

1. marwanhub-cli (هذا المشروع)
   - أداة سطر أوامر متكاملة
   - رابط: https://github.com/marwanhubeg/marwanhub-cli

2. marwanhub-official
   - المستودع الرسمي لـ ماروان هوب
   - رابط: https://github.com/marwanhubeg/marwanhub

3. MH-Templates (قريباً)
   - قوالب مشاريع جاهزة بالعربية
   - قيد التطوير...

⭐ تابعونا على GitHub للمزيد!
`));
  });

// أمر عرض الخدمات
program
  .command('خدمات')
  .description('عرض خدمات ماروان هوب')
  .action(() => {
    console.log(colors.success(`
🛠️ خدمات ماروان هوب:

1. تطوير البرمجيات
   - تطبيقات ويب وموبايل
   - أنظمة إدارة متكاملة

2. الذكاء الاصطناعي
   - حلول ذكاء اصطناعي مخصصة
   - أتمتة العمليات

3. الاستشارات التقنية
   - تحليل وتطوير النظم
   - خطط التحول الرقمي

4. الألعاب والترفيه
   - تطوير ألعاب تعليمية
   - محتوى ترفيهي تقني

📞 للاستفسارات: info@marwanhub.tech
`));
  });

// إذا لم يتم إدخال أمر
if (process.argv.length <= 2) {
  program.help();
}

// تحليل الأوامر
program.parse(process.argv);
